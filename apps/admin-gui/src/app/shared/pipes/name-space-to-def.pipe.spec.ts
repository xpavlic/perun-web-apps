import { NameSpaceToDefPipe } from './name-space-to-def.pipe';

describe('NameSpaceToDefPipe', () => {
  it('create an instance', () => {
    const pipe = new NameSpaceToDefPipe();
    expect(pipe).toBeTruthy();
  });
});
