import { AttributeTypeCleanPipe } from './attribute-type-clean.pipe';

describe('AttributeTypeCleanPipe', () => {
  it('create an instance', () => {
    const pipe = new AttributeTypeCleanPipe();
    expect(pipe).toBeTruthy();
  });
});
