import { ApplicationFormWidgetDescriptionPipe } from './application-form-widget-description.pipe';

describe('ApplicationFormWidgetDescriptionPipe', () => {
  it('create an instance', () => {
    const pipe = new ApplicationFormWidgetDescriptionPipe();
    expect(pipe).toBeTruthy();
  });
});
