import { ApplicationStatePipe } from './application-state.pipe';

describe('ApplicationStatePipe', () => {
  it('create an instance', () => {
    const pipe = new ApplicationStatePipe();
    expect(pipe).toBeTruthy();
  });
});
