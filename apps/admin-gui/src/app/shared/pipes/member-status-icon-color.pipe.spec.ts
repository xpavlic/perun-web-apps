import { MemberStatusIconColorPipe } from './member-status-icon-color.pipe';

describe('MemberStatusIconColorPipe', () => {
  it('create an instance', () => {
    const pipe = new MemberStatusIconColorPipe();
    expect(pipe).toBeTruthy();
  });
});
