import { MemberEmailPipe } from './member-email.pipe';

describe('MemberEmailPipe', () => {
  it('create an instance', () => {
    const pipe = new MemberEmailPipe();
    expect(pipe).toBeTruthy();
  });
});
