import { DisplayedRolePipe } from './displayed-role.pipe';

describe('DisplayedRolePipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayedRolePipe();
    expect(pipe).toBeTruthy();
  });
});
