import { ResourceTagsToStringPipe } from './resource-tags-to-string.pipe';

describe('ResourceTagsToStringPipe', () => {
  it('create an instance', () => {
    const pipe = new ResourceTagsToStringPipe();
    expect(pipe).toBeTruthy();
  });
});
