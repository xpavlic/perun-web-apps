import { MemberStatusIconPipe } from './member-status-icon.pipe';

describe('MemberStatusIconPipe', () => {
  it('create an instance', () => {
    const pipe = new MemberStatusIconPipe();
    expect(pipe).toBeTruthy();
  });
});
