import { MemberCandidateEmailPipe } from './member-candidate-email.pipe';

describe('MemberCandidateEmailPipe', () => {
  it('create an instance', () => {
    const pipe = new MemberCandidateEmailPipe();
    expect(pipe).toBeTruthy();
  });
});
