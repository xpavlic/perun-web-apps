import { ApplicationFormItemTypePipe } from './application-form-item-type.pipe';

describe('ApplicationFormItemTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ApplicationFormItemTypePipe();
    expect(pipe).toBeTruthy();
  });
});
