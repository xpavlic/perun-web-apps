import { ConsolidationErrorPipe } from './consolidation-error.pipe';

describe('ConsolidationErrorPipe', () => {
  it('create an instance', () => {
    const pipe = new ConsolidationErrorPipe();
    expect(pipe).toBeTruthy();
  });
});
